# 非核心功能物理删除报告

日期：2026-06-22 至 2026-06-23

## 完成结果

本轮按模块族连续删除非核心代码，当前工作区累计变更 529 个受版本控制文件，净删除约 15.3 万行。未创建提交，未推送远端。

已物理移除或从运行入口拆除的主要能力：

- 客服/Kefu、CMS/资讯、直播、短视频、移动商户管理、外部服务、票据打印。
- 优惠券、砍价、拼团、秒杀、预售、抽奖、充值、积分商城；签到及必要积分账本保留。
- 收藏、足迹、分享入口、站内信、注销、赠礼、代付、余额中心、发票。
- DIY/主题装修、离线支付、门店自提、核销和高级分销；基础 spread/commission 保留。
- 对应管理端与 UniApp 页面、组件、路由和 API 封装，以及后端路由、控制器、客服长连接与独占入口。

保留微信登录与身份、训练营商品、购物车、普通快递订单、微信支付与回调、退款、基础分销佣金、测评、成长档案、训练进度、签到积分、会员等级，以及后台核心用户、商品、订单、佣金、教育、会员和基础设置。

## 接口与共享服务收缩

- 删除 `MvpRouteBlockMiddleware` 和 `MVP module disabled` 响应；退休接口不再返回业务禁用提示，而由无路由状态自然返回 404。
- 删除失效的 `config/mvp.php` 与前端 MVP 命名开关，改为永久核心范围规则。
- 保留集中式 `config/retired.php` 菜单过滤清单，数据库表、业务数据、安装 SQL、升级元数据及菜单种子均未修改。
- 商品详情继续保持 `coupons: []` 兼容边界，活动字段固定为空。
- 商品共享服务已删除砍价、拼团、秒杀、优惠券价格等永远不会执行的查询分支。
- 购物车新增入口现在只按普通商品处理；旧活动参数会被归一为 0，不再触发秒杀、砍价、拼团、预售服务。
- 支付成功链路已移除抽奖次数副作用；普通订单仍保留微信支付、快递履约、退款、库存和基础佣金所需能力。
- 会员订单创建恢复为最小控制器，仅保留会员订单创建；线下支付等退休分支不会恢复。
- UniApp 小程序构建链增加 Node 24 兼容垫片，仅用于补齐旧 uni 编译依赖需要的 `util.isRegExp`。

## 契约测试

新增 `tests/contracts/retired-surface.contract.mjs`，并提供 `npm run test:retired-contracts`：

- 静态断言退休控制器不再被 API 或管理端路由引用。
- 静态断言不存在 `MVP module disabled` 与退休路由拦截中间件引用。
- 断言退休菜单过滤配置、公共辅助函数、DAO 查询范围和模型查询器完整连接。
- 设置 `TEST_BASE_URL` 后，可对退休 URL 矩阵执行真实 HTTP 404 契约测试。
- 同时设置 `ADMIN_MENU_URL` 与 `ADMIN_TOKEN` 后，可断言真实后台菜单响应不包含退休条目。

本机静态契约测试已通过；基于临时隔离后端的真实退休 URL HTTP 404 合约也已通过。真实后台菜单接口需要管理端 token，本轮保留为可选契约入口，未伪造通过。

## 带数据联调

使用 Docker 恢复 MySQL、Redis、PHP、Nginx 后，从现有脱敏生产结构库复制出 `crmeb_smoke` 临时库进行可回滚联调；测试结束后已删除临时库、清空临时 Redis DB，并恢复原开发容器。

真实接口链路验证结果：

- 登录：账号密码登录成功，生成 API 令牌。
- 商品：商品列表成功；商品详情成功，且 `activity: []`、`coupons: []`。
- 购物车：普通商品加入购物车成功。
- 地址：新增快递地址成功。
- 普通快递订单：订单确认成功，订单创建成功；旧活动参数归一后订单确认仍成功，扣减活动 ID 均为 0。
- 微信支付回调边界：通过保留的 `PayNotifyServices::wechatProduct` 处理测试订单，订单成功置为微信已支付。
- 订单列表/详情：均成功。
- 收货：临时发货后用户确认收货成功。
- 退款：对测试订单申请退款成功。
- 佣金：佣金接口成功。
- 测评：临时库应用既有测评升级表后，测评记录提交成功。
- 签到：临时库打开签到开关后，签到成功并写入积分账本。
- 会员：会员首页接口成功。

说明：本轮没有修改安装 SQL、升级元数据或真实业务库。测评表和签到开关仅在临时 `crmeb_smoke` 联调库中补齐/打开，用于验证保留功能。

## 验证结果

- PHP 语法检查通过。
- `git diff --check` 通过，无空白错误。
- 根目录教育 H5 生产构建通过，受版本控制运行产物已重建到 `crmeb/public/h5`。
- CRMEB 管理端生产构建通过；仅有既有 CSS 顺序与包体积警告，没有缺失模块或悬空导入。
- UniApp 微信小程序生产构建通过；仅有 uni-app/Sass 既有兼容警告，没有缺失页面、组件或导入。
- 退休面静态契约测试通过。
- 退休 URL 真实 HTTP 404 契约测试通过。
- 运行代码中未再发现 MVP 禁用提示、退休拦截中间件或路由占位截断文本。
- 核心订单、支付回调、收货、退款、佣金、测评、签到和会员链路已在临时库完成真实验证。

## 下一步建议

1. 人工走查后台菜单：用真实管理端账号登录后执行 `ADMIN_MENU_URL` + `ADMIN_TOKEN` 的菜单契约，确认数据库旧菜单不会通过接口回显。
2. 准备提交前再做一次代码审阅，重点看会员订单、退款、佣金、签到积分和商品详情这些保留边界。
3. 第二轮孤儿服务/验证器/语言项清理已完成；后续继续压缩时，建议先处理仍被通知、任务、退款或公共接口引用的旧模块调用点，再删除对应服务链。
4. 确认后再由你决定是否分批提交；当前工作区仍保持未提交、未推送状态。



## 第二轮孤儿代码清理（2026-06-23）

本轮按“无路由、无构建引用、无保留数据修复依赖”的安全边界继续压缩，只处理已经没有运行时代码引用的历史服务链、验证器和语言项；没有提交、没有推送，也没有修改数据库表、业务数据、安装 SQL、升级元数据或菜单种子。

本轮新增物理删除：
- 旧营销/直播/门店/客服相关后台验证器 10 个：直播、砍价、拼团、优惠券、积分商城、秒杀、门店、客服话术等已无入口校验类。
- 历史孤儿服务 10 个：优惠券残留服务、DIY/页面分类/页面链接/主题模块残留服务、客服反馈/话术残留服务、模板消息残留服务。
- 对应孤儿 DAO 9 个与模型 6 个：优惠券残留、DIY 页面/主题模块、客服反馈/话术、模板消息等已经完全断开的数据访问与模型类。
- 语言包未引用 key 53 个：仅移除在 PHP 运行时代码中没有任何引用的旧模块提示，保留订单、退款、登录、通知、签到积分、会员与核心后台仍可能使用的提示。

本轮验证：
- 被删类名运行时代码扫描通过：除升级元数据外，没有 PHP 运行时代码再引用本轮删除的类。
- app/lang/zh_cn.php 与 app/lang/en_us.php 语法检查通过。
- 后端 app、config、route 范围 PHP 语法检查通过，共检查 829 个 PHP 文件。
- git diff --check 通过，仅保留既有 LF/CRLF 提示，无空白错误。
- npm run test:retired-contracts 通过；当前未设置运行时服务地址，所以真实 HTTP 404 与后台菜单运行时检查按合同脚本规则跳过。

第二轮之后仍建议继续保守处理的区域：
1. 还有一些旧模块服务类仍被保留代码、定时任务、通知、退款或公共接口间接引用，不能按本轮规则直接删除；下一轮应先收缩这些调用点，再删除对应服务链。
2. 前端仍有少量不可达或已隐藏的历史 API 封装、静态资源和展示文案；建议另开一轮“无页面路由、无 import、无构建引用”的前端资源清理。
3. 真实后台菜单接口仍建议用管理员 token 跑一次运行时菜单合同，确认数据库旧菜单不会回显。
