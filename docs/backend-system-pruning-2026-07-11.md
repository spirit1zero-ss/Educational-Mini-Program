# 后端系统维护代码精简 - 2026-07-11

## 本轮结果

- 物理删除 27 个 PHP 文件。
- `crmeb/app` 从 715 个 PHP 文件、4.51 MB 降至 688 个、4.27 MB。
- `crmeb/app/adminapi` 当前为 86 个 PHP 文件、约 0.37 MB。
- 排除本地 `runtime` 后，项目当前约 175.87 MB。

## 删除内容

- CRUD/代码生成控制器、服务、DAO、模型和全部后台路由。
- 服务器文件浏览、在线编辑、文件 MD5 校验控制器、中间件、服务、DAO 和模型。
- 在线升级服务、升级队列任务、升级日志 DAO/模型和旧升级开放接口客户端。
- 清空业务数据、批量域名替换和旧 APP 版本管理控制器及路由。
- 将 47 KB 的旧商城 `PublicController` 收缩为轻量健康检查和站点备案配置，移除其对商品、订单、营销、装修和客服服务的直接依赖。

图片上传、图片素材选择、数据库备份、系统路由、缓存维护、管理员权限和定时任务配置没有随服务器文件管理一起删除。

升级目录中的 SQL 文件继续保留，因为其中包含会员、产品有效期和分销推荐关系的数据库迁移。

## 验证结果

- 小程序冒烟测试：通过。
- 管理后台退役界面契约：通过。
- 后端退役接口与本地类导入契约：通过。
- Docker 内项目自有 PHP 全量语法检查：通过。
- `/api/index`：200，返回轻量服务健康信息，不再查询旧商城首页商品和活动数据。
- 分销收益与分销管理路由：存在，未登录时正确返回 401 业务状态。
- 清缓存路由：存在，未登录时正确返回 401 业务状态。
- CRUD、服务器文件管理和 APP 版本管理接口：404。

## 下一阶段边界

剩余体积较大的传统商城模块为：

- activity：70 个文件，约 579.9 KB。
- product：59 个文件，约 326.7 KB。
- order：45 个文件，约 633.3 KB。

这些模块仍被后台首页统计、`UserServices` 的旧商城个人中心/优惠券方法、支付监听器和若干任务交叉引用。下一阶段需要先把保留入口改为教育小程序专用实现，再逐层删除，不能直接整目录移除。

## 商城依赖解耦进展

- 后台首页移除订单趋势和商品购买排行入口。
- 首页头部指标改为新增用户、分销员、待审核提现和用户佣金余额。
- 后台待办提醒只保留提现审核，不再查询待发货订单、库存预警和商品评论。
- `UserServices` 删除未被当前路由调用的旧商城个人中心、活动状态、签到记录和优惠券记录逻辑。
- 后台新建用户不再发放商城优惠券；旧新人礼控制器方法已移除。
- 用户详情不再展示签到和优惠券标签页。
- 当前后端约 688 个 PHP 文件、4.21 MB；项目排除 `runtime` 后约 175.80 MB。

本阶段本地三项契约检查已通过。Docker PHP 和 HTTP 复测因 Codex Docker 工具额度暂时受限，需在限制恢复后补跑；在补跑前不继续物理删除 activity/product/order 目录。
