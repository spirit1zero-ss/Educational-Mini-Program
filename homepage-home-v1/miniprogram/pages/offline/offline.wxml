<view class="page">
  <view class="custom-nav" style="{{navStyle}}">
    <view class="nav-title">线下门店</view>
  </view>

  <scroll-view class="offline-scroll" style="{{scrollStyle}}" scroll-y enhanced show-scrollbar="{{false}}">
    <view class="content">
      <view class="summary-card">
        <view class="summary-head">
          <view>
            <view class="summary-kicker">门店位置</view>
            <view class="summary-title">线下体验点展示</view>
            <view class="summary-desc">查看开放中的线下体验点，获取训练营咨询、学习规划与家长沟通服务。</view>
          </view>
          <image class="summary-icon" src="{{heroIcon}}" mode="aspectFit"></image>
        </view>
      </view>

      <view class="store-panel">
        <view class="section-title">门店列表</view>
        <view class="store-empty" wx:if="{{loading}}">正在加载线下地址...</view>
        <view class="store-empty" wx:elif="{{!stores.length}}">暂无线下体验点，请稍后再来查看</view>
        <view
          class="store-card"
          wx:for="{{stores}}"
          wx:key="id"
          bindtap="onStoreTap"
          data-item="{{item}}"
        >
          <view class="store-top">
            <view>
              <view class="store-name">{{item.name}}</view>
              <view class="store-city">{{item.city}}</view>
            </view>
            <view class="store-status">{{item.status}}</view>
          </view>
          <view class="store-address">{{item.address}}</view>
          <view class="store-service">{{item.service}}</view>
          <view class="store-meta" wx:if="{{item.businessHours}}">营业时间：{{item.businessHours}}</view>
          <view class="store-actions">
            <view class="store-action">查看位置 ›</view>
            <view
              class="store-action store-action--phone"
              wx:if="{{item.phone}}"
              catchtap="onCallPhone"
              data-phone="{{item.phone}}"
            >电话咨询</view>
          </view>
        </view>
      </view>

      <view class="notice-card">
        <view class="notice-title">到店提醒</view>
        <view class="notice-line">建议到店前先电话联系，确认营业时间和接待安排。</view>
        <view class="notice-line">点击体验点可打开地图导航；未配置坐标时会复制详细地址。</view>
      </view>
    </view>
  </scroll-view>

  <view class="bottom-tab">
    <view
      class="tab-item {{item.key === 'offline' ? 'tab-item--active' : ''}}"
      wx:for="{{tabs}}"
      wx:key="key"
      bindtap="onTabTap"
      data-item="{{item}}"
    >
      <image class="tab-icon" src="{{item.key === 'offline' ? item.activeIcon : item.icon}}" mode="aspectFit"></image>
      <view class="tab-text">{{item.text}}</view>
    </view>
  </view>
</view>
